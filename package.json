{
  "name": "cursor-tracker",
  "private": true,
  "version": "0.3.0",
  "description": "Local Cursor usage agent + dashboard",
  "scripts": {
    "agent:sync": "npm --prefix cursor-usage-agent/agent run sync",
    "dashboard": "npm --prefix cursor-usage-agent/dashboard run dev",
    "dev": "npm run dashboard"
  },
  "dependencies": {
    "@neon/config": "^1.5.0",
    "@neon/env": "^1.3.4"
  }
}
